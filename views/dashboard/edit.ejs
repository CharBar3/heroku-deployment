<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/nav.ejs') %> 
    <h1><%= workoutDay.title %> EDIT</h1>
    <div>
    <% for(let i = 0; i < workoutDay.exercises.length; i++) { %>
        <% const workoutExercise = workoutDay.exercises[i] %> 
        <div><%= workoutExercise.name %></div> 
        <% const setsRepsWeights = workoutExercise.setsRepsWeights %> 
        <% for(let i = 0; i < setsRepsWeights.length; i++) { %>
        <div>Set <%= i+1 %>: Reps <%= setsRepsWeights[i].reps %> Weight <%= setsRepsWeights[i].weight %></div>
        <% } %>
    <% } %>
    </div> 